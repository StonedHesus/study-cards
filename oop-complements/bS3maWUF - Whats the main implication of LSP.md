What's the main `implication` of `LSP`?
---
`LSP` implies `polymorphism`, i.e. instances of `subclasses` should be usable in place of `instances` of `superclasses`, without impacting the functionality of the software.