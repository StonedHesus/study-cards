What is the `side-effect` of `nominal typing`?
---
The `side-effect` of `nominal typing` is that if `two types` have `different names`, although they have the same `underlying structure` then they are considered `distinct`.